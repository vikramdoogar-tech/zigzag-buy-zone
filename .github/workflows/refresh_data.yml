name: Refresh ZigZag Data

on:
  workflow_dispatch:
  schedule:
    - cron: "20 11 * * 1-5"   # 16:50 IST, Mon-Fri

permissions:
  contents: write

jobs:
  refresh:
    runs-on: ubuntu-latest
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          persist-credentials: true

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Show files
        run: |
          pwd
          ls -la

      - name: Run refresh script
        run: |
          python refresh_data.py

      - name: Replace JSON
        run: |
          if [ -f data_rebound.refreshed.json ]; then cp data_rebound.refreshed.json data_rebound.json; fi
          if [ -f data_watchlist.refreshed.json ]; then cp data_watchlist.refreshed.json data_watchlist.json; fi

      - name: Commit changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data_rebound.json data_watchlist.json
          git diff --cached --quiet && echo "No changes to commit" && exit 0
          git commit -m "Auto refresh data"
          git push
